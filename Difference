#include <stdio.h>

int main() {
    int n,pro=1,sum=0,r;
    printf("enter n value");
    scanf("%d",&n);
    while(n!=0)
    {
        r=n%10;
        pro=pro*r;
        sum=sum+r;
        n=n/10;
    }
printf("%d",pro-sum);
    return 0;
}
